# 实例：DSH 科普片（dsh-harness-001）

主题：DeepSeek 新出的 Harness（DSH）是什么、和以前的 agent 有什么不同。

## 专家金句

> Agent 的未来，不只拼大脑多聪明，更拼工作台换错零件后能否收场。

## 脚本（7 屏，18 句，约 414 字）

见 `script.txt`（旁白）与 `script.json`（分屏）。

## 最终成片参数

- 1080×1920，25fps，74.64 秒
- H.264 + AAC（24000Hz 单声道）
- 配音：百炼 `qwen-audio-3.0-tts-plus` / `longanlingxin`，1.19 倍速
- 数字人：Live2D Haru，半身 250px 宽，右上角 `W-w-40:165`
- 音效：程序化合成，0.3 权重混音

## 踩坑记录

1. 数字人全身构图太小、遮挡 → 改半身（调 anchor/scale/x/y），缩到 250px。
2. 配音初版 75.15s 超目标 → 统一 atempo 1.18→1.19 压到 74.64s。
3. 口型是逐句区间驱动的张合，非音素级唇形（简单版边界）。
4. 第 1/3/6 屏旧图示与新文案语义不匹配 → 需重画。

## 复现路径

按 `SKILL.md` 全链路，产物依赖 `skills/` 各子技能。
